// Canvas Hook - Easy access to Canvas Context
import { useCanvas as useCanvasContext } from '../contexts/CanvasContext';

// Re-export the canvas hook for convenience
export const useCanvas = useCanvasContext;
